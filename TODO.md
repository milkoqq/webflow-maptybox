# Change ARROW to + SVG in [Webflow] x

# Fix a little bit the styling [Webflow] x

# Styling of Mapbox Popups [JS/CSS/Mapbox API]

# Add Toast boxes for sorting. If no workouts, shouldn't work [Javascript] x
